#include <iostream>
using namespace std;
int main(){
    // bitwise & 
    // both values of binary format must be true to genrate one
    // bitwise |
    // one of the value of binary format is one than the output will be one
    // bitwise ^ (XOR)
    // bits are same ->0
    // bits are diff ->1
    // << left shift
    // a << b is equal to ==->>> a * pow(2,b);
    // 5<<2
    // 00101 = 5
    // 10100 -> 20
    // >> right shift
    // a >> b is equal to ==->>> a / pow(2,b);
    // 20>>2
    // 10100 = 20
    // 00101 -> 5

    // operators precedence 
    // ! , + , - (unary operators)  R To L
    // * , / , %                    L To R
    // + , -                        L To R
    // < , <= , >= , >              L To R
    // == , !=                      L To R
    // &&                           L To R
    // ||                           L To R
    // =                            R To L
    
    return 0;
}