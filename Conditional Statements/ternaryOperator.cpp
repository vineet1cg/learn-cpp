#include <iostream>
using namespace std;
int main (){
    // ternary statement to check conditions
    // condition ? true outcome : false outcome;
    int n = 5;
    cout<<(n>2 ? "Printed On True Outcome" : "Printed On False Outcome") << endl;
    return 0;
}